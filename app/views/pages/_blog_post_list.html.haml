#blog-posts
  %h1= "blog posts  #{fa_icon("pencil", class: "pencil-icon")}".html_safe
  - @blog_posts.each do |post|
    .blog_post
      %h2#post-title
        = link_to post.title, blog_post_path(post.slug)
      #post-date
        = format_date(post.created_at)

    %hr
