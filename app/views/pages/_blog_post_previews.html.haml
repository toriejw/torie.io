#blog-posts
  - @blog_posts.each do |post|
    %h2
      = link_to post.title, blog_post_path(post.id)

    = @markdown.render(post.preview).html_safe
    = link_to "Contine reading", blog_post_path(post.id)
    %br
    %br
